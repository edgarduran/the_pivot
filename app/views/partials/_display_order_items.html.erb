<% order.order_items.each do |item| %>
  <tr>
    <td>
      <%= item.car.full_name %>
    </td>
    <td>
      <%= link_to item.store_name, store_cars_path(store: item.store_slug) %>
    </td>
    <td>
      <%= item.current_status %>
    </td>
  </tr>
<% end %>
